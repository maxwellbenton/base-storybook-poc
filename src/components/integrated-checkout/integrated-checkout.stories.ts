export default {
  // this creates a ‘Components’ folder and a ‘MyComponent’ subfolder
  title: 'Components/IntegratedCheckout',
};

const Template = () => `<integrated-checkout></integrated-checkout>`;

export const Example = Template.bind({});